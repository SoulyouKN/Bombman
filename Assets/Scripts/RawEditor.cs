using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RawEditor : MonoBehaviour {

	// Use this for initialization
	void Start () {
		GameObject.Find("公告").SetActive(false);
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
